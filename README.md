# Currency Converter
A command-line tool that converts any country's currency to BRL

## Features
- Real time exchanges based on updated currencies
- Supports any currency
- Logs every conversion with timestamp to a .txt file

## Tecnologies
- Python 3
- ExchangeRate API
- Requests library

## How to run

Install dependencies:
pip install requests

Run the script:
python conversor.py

